# Build Configuration
- Use cmake with ninja generator
- The only CMakeLists.txt manages build at the project root
- Use clang-19 for build
- Look for an existing build folder and use it to speedup build (currently it is cmake-build-debug-clang-19)
